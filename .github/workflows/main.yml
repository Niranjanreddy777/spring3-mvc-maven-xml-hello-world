name: CI with maven and tomcat


on:
  push:
    branches: [ master ]



jobs:
   build:
    
    runs-on: self hosted

    
    steps:
      - uses: actions/checkout@v2
      - name: java version 
        run: java --version

